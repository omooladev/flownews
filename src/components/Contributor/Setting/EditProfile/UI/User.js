import Card from "../../../../../UI/Card";
import styles from "./User.module.css";
const User = () => {
  return (
    <Card className={styles.user}>
      <h2>User</h2>
    </Card>
  );
};

export default User;
