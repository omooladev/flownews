import styles from "./PreviewStory.module.css";
const PreviewStory = () => {
  return (
    <div className={styles["preview-area"]}>
      <button>Remove Preview</button>
    </div>
  );
};

export default PreviewStory;
